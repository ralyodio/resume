const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('node:fs'); const os=require('node:os'); const path=require('node:path'); const {selectResume}=require('../src/resumes/select-resume.cjs');
test('selects variant if present else fallback',()=>{ const dir=fs.mkdtempSync(path.join(os.tmpdir(),'resume-')); const fb=path.join(dir,'anthony.ettinger.resume4.pdf'); fs.writeFileSync(fb,'x'); const ai=path.join(dir,'resume-ai-engineer.pdf'); fs.writeFileSync(ai,'x'); assert.equal(selectResume({title:'LLM Engineer'}, {resumeDir:dir,fallbackResume:fb}), ai); assert.equal(selectResume({title:'Web3 Engineer'}, {resumeDir:dir,fallbackResume:fb}), fb); });
