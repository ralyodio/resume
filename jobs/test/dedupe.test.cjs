const test=require('node:test'); const assert=require('node:assert/strict'); const {isDuplicate,mergeJobs}=require('../src/normalize/dedupe.cjs');
test('canonical apply URL duplicate and easier mode wins',()=>{ const a={id:'1',source:'a',title:'Engineer',company:'Co',location:'Remote',applyUrl:'https://x.com/job?utm_source=a',applicationMode:'external-ats'}; const b={id:'2',source:'b',title:'Engineer',company:'Co',location:'Remote',applyUrl:'https://x.com/job',applicationMode:'easy-apply',salaryMin:1}; assert.equal(isDuplicate(a,b),true); const m=mergeJobs(a,b); assert.equal(m.applicationMode,'easy-apply'); assert.equal(m.salaryMin,1); });
